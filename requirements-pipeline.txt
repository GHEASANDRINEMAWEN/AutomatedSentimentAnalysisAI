# Full data-collection + analysis pipeline (run.py).
# The dashboard alone only needs requirements.txt.
praw
requests
serpapi
youtube-transcript-api
transformers
torch
sentencepiece  # XLM-RoBERTa (multilingual sentiment) tokenizer needs this
pandas
